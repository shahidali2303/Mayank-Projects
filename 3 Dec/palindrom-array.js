const numbers = [1, 2, 3];

// array conveted to string
// join( ) => returns an array as a string

const joined = numbers.join("");
console.log(joined);

// string converted to array

const message = "This is a message";
const parts = message.split(" ");
console.log(parts);
